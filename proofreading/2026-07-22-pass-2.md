# Proof-reading pass 2026-07-22 (2nd)

Scheduled proof-reading routine — one agent per language, 25 random exercises each (EN + FR), reviewing both content (grammar/spelling/accents/answer correctness) and structure (schema + engine rendering).

## Findings & fixes

### English — 6 items removed (near/exact duplicates)
- `articles-a1.json` — removed 2 bare-article near-dups ("I like {1} dogs.", "I drink {1} water every morning." → "—") that admitted a defensible `the` reading; kept the disambiguated "In general, …" twins.
- `vocabulary-c1.json` — removed 4 British-spelling clones appended over the file's otherwise-consistent American set (coherent / synthesise / inadvertently / distil).

### French — 4 items removed, 61 text fields fixed
- `hypothese-irreel-passe-b2.json` — **structural**: 61 `fill` items embedded the conjugation cue inline right after the placeholder (e.g. `{1} (rester)`), which the fill renderer already prints — so the cue rendered twice. Stripped 120 inline parentheticals across those 61 `text` fields. Answers/grammar verified correct and left unchanged.
- `negation-a1.json` — removed 2 near-duplicate "il n'y a {1} (ici)." → personne items.
- `partitive-articles-a2.json` — removed 1 near-duplicate "{1} argent" → "de l'" item.
- `vocabulary-nouns-a2.json` — removed 1 duplicate "{1} sur le balcon" → vue item.

## Validation
`node scripts/build-manifest.mjs` → OK (en: 48 exercises / 1167 items · fr: 41 / 1332 items). All touched files valid JSON.

## Verification pass over existing PR #106 changes
Ran a verification agent over the changes already in the PR. All prior edits and duplicate-removals checked out except one net-new regression, now fixed:
- `negation-a1.json` — two kept cloze items (`Je ne mange {1} de viande…`, `Elle ne boit {1} d'alcool…`, answer `jamais`) listed `pas` as a distractor, but `ne… pas` is equally correct in both — a two-correct-answer bug (learner choosing `pas` wrongly marked wrong). Replaced the `pas` distractor with `personne` (clearly wrong here) in both. No other net-new defects found across the 29 touched files.
