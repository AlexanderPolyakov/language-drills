# Proof-reading pass — 2026-07-21 (second pass)

Scheduled proof-reading routine, appended to the single rolling proof-reading
PR (#106). One agent per language, **25 random exercises each** (EN + FR),
reviewing both **content** (grammar / spelling / accents / answer correctness /
ambiguity) and **structure** (schema conformance + how each item renders in the
engine).

## Random sample reviewed

- **EN (25):** articles-geography, adverbs-frequency-a2, possessive-adjectives-a1,
  cleft-sentences-c1, demonstratives-a1, plurals, vocabulary-adjectives-a2,
  vocabulary-adjectives-a1, gerunds-infinitives-b2, concessive-clauses-c2,
  articles-expressions, ellipsis-substitution-c2, past-continuous-b1, questions,
  nuanced-modality-c2, articles-a1, articles-basic, passive-voice-b1,
  reported-speech-b2, mixed-conditionals-b2, prepositions-time-place,
  vocabulary-b2, vocabulary-b1, there-is-there-are-a1, vocabulary-nouns-a2.
- **FR (25):** questions-a1, adjective-agreement-a2, passe-simple-c1, ir-verbs-a2,
  vocabulary-verbs-a1, connecteurs-logiques-b2, vocabulary-adjectives-a2,
  concordance-des-temps-c2, conditional-b1, vocabulary-verbs-a2,
  partitive-articles-a2, subjonctif-imparfait-c2, prepositions-a2,
  gerondif-participe-present-b2, imparfait-vs-passe-compose-b1,
  double-pronominalisation-c2, nominalisation-c1, negation-a1,
  possessive-adjectives-a1, gender-number-a1, subjonctif-passe-b2,
  vocabulary-nouns-a1, futur-proche-a2, passe-compose-a2, futur-simple-b1.

## Changes — 6 items removed, 44 fields/items edited across 6 files

### English

| File | Change | Why |
|---|---|---|
| `articles-expressions.json` | removed 1 duplicate item | item[13] was an exact duplicate of item[2] (`He plays {1} piano … skips {2} dinner … {3} radio.`) — identical answers (`the`/`—`/`the`) and explanation. Kept the canonical first copy. |

The other 24 EN files were reviewed and found clean. Things checked but
deliberately left as-is (not defects): intentional British/American dual-answer
acceptance in `mixed-conditionals-b2` / `reported-speech-b2`; internally
consistent American spelling in `vocabulary-b2`; `its` as the standard A1 animal
possessive in `possessive-adjectives-a1`; well-disambiguated modality carriers
in `nuanced-modality-c2`.

### French

| File | Change | Why |
|---|---|---|
| `passe-simple-c1.json` | stripped inline `(cue)` from **42** `text` fields | This is a `fill` exercise; the engine already renders `blanks[].cue` in parentheses beside the input, so an inline `{1} (courir)` displayed the cue **twice**. The file's first items already followed the correct cue-less pattern. All passé simple conjugations, hints and explanations verified correct; only the redundant substring was removed. |
| `negation-a1.json` | removed 3 duplicate items | item[34]/[35]/[37] duplicated item[11]/[0]/[7] (same answer + explanation, differing only in a distractor). Kept the originals; item[36]/[38] confirmed distinct and kept. |
| `double-pronominalisation-c2.json` | removed 1 duplicate item | item[21] had the same prompt + instruction as item[3] (`Le guide a conduit les touristes au sommet`). Kept item[3], which has the fuller accepted-answer list (3 variants vs 2). |
| `vocabulary-verbs-a2.json` | removed 1 duplicate + fixed 1 cue | item[34] duplicated item[20] (`ranger`). On the surviving item[20], corrected the cue `to pack` → `to tidy / put away` — *ranger* = tidy/put away (to pack = *faire ses valises*); this now aligns cue, answer and the item's own explanation. |
| `vocabulary-adjectives-a2.json` | added feminine answer | item[8] `Je suis {1} après cette longue journée.` accepted only `fatigué`; the subject `Je` has no stated gender, so a female learner's correct `fatiguée` was wrongly rejected (the `fill` matcher lowercases but accents matter). Added `fatiguée` and noted it in the explanation. |

The other 20 FR files were reviewed and found clean — including careful checks
of partitive vs bare-`de` (incl. negation → `de`/`d'`) in
`partitive-articles-a2`, adjectif-verbal vs participe-présent spellings in
`gerondif-participe-present-b2`, and past-participle agreement in
`subjonctif-passe-b2`.

## Verification of the existing PR

A separate verification agent re-reviewed every change already committed in this
PR (commit `20225eb`): all 12 deletions leave a correct surviving twin and all
edits are grammatically/orthographically correct with the answer present in
options — **0 regressions, 0 net-new findings** in the files that commit
touched.

## Validation

- `node scripts/build-manifest.mjs` → `OK` (en: 48 exercises / 1178 items ·
  fr: 41 / 1336 items).
- Structural validator: **0 problems** across all 6 touched files; **0
  duplicate item texts** in the 50 reviewed exercises.
- Every edit is byte-minimal; retained items are unchanged and every touched
  file remains valid JSON.

## Deferred (outside this pass's random sample)

Duplicate item texts still exist in a few files **not** drawn in this pass's
random sample — `en/used-to-b1` (3), `en/vocabulary-c1` (3) — matching the
known-deferred list from the first 2026-07-21 pass. Left for a future pass to
keep this one scoped to its assigned 25 + 25 exercises.
